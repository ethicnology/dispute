name: nostr_wrapper
description: Nostr abstraction layer wrapping dart-nostr
publish_to: none
version: 0.1.0+1
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  nostr:
    git:
      url: https://github.com/ethicnology/dart-nostr.git
      ref: develop
  drift: ^2.32.0
  sqlite3: ^3.0.0
  path_provider: ^2.1.0
  path: ^1.9.0

dev_dependencies:
  drift_dev: ^2.32.0
  build_runner: ^2.4.0
