name: dispute
description: A cross-platform Nostr client
publish_to: none
version: 2.0.0+1
resolution: workspace

environment:
  sdk: ^3.11.0

dependencies:
  flutter:
    sdk: flutter
  plugin_interface:
    path: ../../packages/plugin_interface
  nostr_wrapper:
    path: ../../packages/nostr
  wizard:
    path: ../../packages/wizard
  flutter_bloc:
  drift: ^2.32.0
  sqlite3: ^3.0.0
  path_provider: ^2.1.0
  path: ^1.9.0
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  drift_dev: ^2.32.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
